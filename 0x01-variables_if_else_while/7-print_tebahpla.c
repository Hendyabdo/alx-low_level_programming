#include <stdio.h>
/**
* main - Prints the alphabet in lowercase.
*
* Return: Always 0.
*/
int main(void)
{
char letetr;
for( letetr='z'; letter >= 'a'; letter--)
putchar(letetr)
putchar('\n')
return(0);
}
